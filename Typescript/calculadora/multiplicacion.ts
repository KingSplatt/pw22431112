const multiplicar = (a: number, b: number, imprimir: string) => {
    console.log(imprimir, a * b);
}
import { Operaciones } from './calculadora';
multiplicar(2, 4, "el resultado es:");
multiplicar(1, 4, "el resultado es:");